# Docling Wheel Builder

This directory contains a Docker-based setup to build the docling wheel for GraalPy with controlled resource limits.

## Usage

To build the docling wheel:

```bash
./docker-build.sh
```
